package com.rpcwebservice.repositories;

import com.rpcwebservice.entities.TipoSociedad;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoSociedadRepository extends JpaRepository<TipoSociedad,Integer> {

}
